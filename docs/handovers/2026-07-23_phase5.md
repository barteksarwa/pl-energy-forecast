# Handover — 2026-07-22/23: deep-history campaign + Phase 5

## Results (all in docs/RESULTS.md, committed)

- Deep-history campaign (24h batch): window sweep → **1095d best
  (17.38, DM p=0.0009 vs 365d, coverage holds)** — promotion awaits
  owner. Crisis 5-yr: edge holds (rMAE 0.644). Deep full-2yr:
  TFT-730 19.52, PatchTST-730 22.25 — the 1-yr 18.31 was calm-year
  flattered; per-year tables in the campaign report.
- Statistical tests added (Lago protocol): DM daily-multivariate,
  Kupiec, Christoffersen. Honest hit: **LGBM vs LEAR p=0.056 — not
  significant.** Bands fail Christoffersen (violations cluster).
- GPD tail: rejected on pre-declared gates (third tail method down).
- Spike classifier: AUC 0.966, promoted, now a daily-report line
  (`p(top-5%)`); deterministic → seed confirm vacuous (documented).
- Chronos-Bolt zero-shot: rMAE 0.787, beats trained PatchTST,
  fine-tune gate closed. CQR on top: 79.9% coverage.

## Public repo

Fully republished through Phase 3 + curated docs + honest-structure
README. CI cron live, committing daily reports. Machinery + commit
logic: `~/Documents/repo-reset/COMMIT_MAP.md`.

## Owner decisions pending

1. Promote 1095d training window to champion config (evidence complete).
2. Soften public README "beats the standard" (LEAR gap not significant).
3. Phase 6 go (TimesFM/Moirai + CRPS ensemble; deep 730d 2-yr already done).

## Gotchas for next session

- `log_gaps` now returns only NEW gaps (was: all, spamming logs).
- Spike classifier lives in the daily loop behind try/except — its
  failure lands in oddities, never kills the run.
- Chronos runs need `uv run --extra fm` (transformers stack optional).
- Campaign scripts resume-safe; status: `bash campaign_status.sh`.
